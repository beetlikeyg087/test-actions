name: Test action

on:
  push:
    branches: [ master ]
jobs:
    test-action:
        name: 'Test actions'
        runs-on: ubuntu-latest
        steps:
            - name: 'Checkouts'
              uses: actions/checkout@master
            - name: 'Test curl actiosn'
              uses: beetlikeyg087/curl@master
              with:
                accept: 201
                is_debug: true,
                custom-config: .github/workflows/curl-config-not-found.json
                
                
                
  
