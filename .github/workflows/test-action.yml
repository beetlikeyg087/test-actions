name: Test action

on:
  push:
    branches: [ master ]
jobs:
    test-action:
        name: 'Test actions'
        runs-on: ubuntu-latest
        steps:
            - name: 'Checkout'
              uses: actions/checkout@master
            - name: 'Test curl action'
              uses: beetlikeyg087/curl@master
              with:
                url: 'https://reqres.in/api/users/2'
                method: 'GET'
                accept: 200
                proxy-url: 180.183.28.208:8080
                timeout: 10000
                
                
                
  
